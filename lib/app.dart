import 'package:flutter/material.dart';
import 'package:loop_habit_tracker/presentation/screens/main_screen.dart';

class App extends StatelessWidget {
  const App({super.key});

  @override
  Widget build(BuildContext context) {
    return const MainScreen(); // Main content of the app
  }
}
